프로그래밍 언어 : 루아

메이플월드에서 준비해놓은 api

API : 메이플 월드 개발자 분들이 편하게 게임을 만들 수 있도록 미리 만들어 놓은 코드

몬스터를 소환하거나, 아이템을 착용하는 등 게임 내에서 유용한 기능이 쉽게 구현될 수 있도록 준비된 코드이다.

루아문법

컴포넌트의 변수는 자료형 변수

지역변수는 local 변수

같은 Entity의 프로퍼티를 가져오려면 self.Entity.TransformComponent.Position.x

주석처리는 --

다른 Entity의 프로퍼티를 가져오려면 _EntityServie.GetEntityByPath

프로퍼티는 .으로 가져오고, 메서드는 :로 가져온다.

클라이언트가 아닌, 서버에서 이벤트를 실행하기 위해서는 이벤트를 함수로 만들고, 함수를 서버에서 실행하게끔 하면 된다.
